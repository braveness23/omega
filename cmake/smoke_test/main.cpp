#include <omega/export.h>
#include <omega/omega.h>

// Minimal consumer: verifies that find_package(omega) works, headers parse,
// and the binary links. Does not call any unimplemented API functions.
int main()
{
    return 0;
}
