#include <catch2/catch_test_macros.hpp>

// Placeholder so the test target compiles before real tests are written.
// Remove this file when unit/test_timing_model.cpp is added.

TEST_CASE("placeholder passes") {
    REQUIRE(1 + 1 == 2);
}
